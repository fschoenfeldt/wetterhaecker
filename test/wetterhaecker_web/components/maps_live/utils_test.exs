defmodule WetterhaeckerWeb.Components.MapsLive.UtilsTest do
  use ExUnit.Case, async: true

  import Phoenix.LiveViewTest

  alias Phoenix.LiveView.Upload
  alias WetterhaeckerWeb.Components.MapsLive.Utils

  @tag :todo
  test "add_time_and_weather processes points with weather data" do
    # TODO: Test the add_time_and_weather function
    # Hint: Check if weather data is correctly added to the points
  end
end
